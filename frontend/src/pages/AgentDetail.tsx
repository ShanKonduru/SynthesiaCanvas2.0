import { Box, Typography } from '@mui/material';

export default function AgentDetail() {
  return (
    <Box>
      <Typography variant="h4">Agent Detail</Typography>
      <Typography>Agent configuration and performance metrics</Typography>
    </Box>
  );
}
